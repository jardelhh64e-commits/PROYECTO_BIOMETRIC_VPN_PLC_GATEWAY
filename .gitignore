# Secretos — nunca subir al repo
*.env
.env
token.env
*.secret

# El .env.example SÍ se sube (es solo una plantilla sin datos reales)
!.env.example

# Python
__pycache__/
*.pyc
*.pyo

# Carpeta de documentos local (no se sube)
docs/
*.pdf

# Logs
*.log
